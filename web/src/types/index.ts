/**
 * Shared global types only. Feature-specific types live in features/<name>/types.ts.
 * Do not add auth types here – use @/features/auth.
 */

// Add shared types (e.g. Id, DateRange) as needed. Auth: use @/features/auth.
